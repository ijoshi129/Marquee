node_modules/
.env
.env.local
dist/
build/
*.log
.DS_Store

# Operations: never commit dumps
backups/
docker/backups/

# Real AMC emails used for local parser testing — contain PII (email
# address, order numbers, names). Sanitize before adding to the repo.
fixtures/
